import 'package:flutter/material.dart';

class DirectionsTabView extends StatelessWidget {
  const DirectionsTabView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Clinic Directions"),
    );
  }
}
